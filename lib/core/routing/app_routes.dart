/// AppRoutes — all route path constants for the application.
/// Every path is prefixed with `k` and defined as a static const String.
abstract final class AppRoutes {
  static const String kBookingView = '/';
}
