/// AppConstants — app-wide string and numeric constants.
/// Add new shared values here rather than hardcoding them across the codebase.
abstract class AppConstants {
  // TODO: change these values — set your desired font family name and register
  // it in pubspec.yaml under the flutter > fonts section, or add a Google Font.
  static const String appFamilyFont = '';

  static const String appName = 'Appointment Booking';
}
